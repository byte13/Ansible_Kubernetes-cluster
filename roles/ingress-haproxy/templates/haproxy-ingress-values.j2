controller:
  ingressClassResource:
    enabled: true
  kind: DaemonSet
  # Expose HAProxy service via host port on all cluster nodes
  #daemonset:
  #  useHostPort: true
  service:
    type: NodePort
    nodePorts:
      http:  {{ centos_haproxy_http_nodeport }}
      https: {{ centos_haproxy_https_nodeport }}
